<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <!-- 管理者がいなくならないように管理者自身は消せないようになっている　-->
  <% if current_user.admin? && !current_user?(user)%>
    | <%= link_to "delete", user, data: { "turbo-method": :delete,
                                          turbo_confirm: "You sure?" } %>
  <% end %>
</li>